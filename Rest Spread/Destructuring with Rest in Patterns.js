// Use array destructuring to divide a list of numbers into a head and a tail
const [head, ...tail] = [1, 2, 3, 4, 5];

// Use object destructuring with rest syntax
const { a, ...rest } = { a: 1, b: 2, c: 3 };
