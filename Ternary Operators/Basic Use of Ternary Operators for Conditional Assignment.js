// Create a variable age and assign a number to it
const age = 20;

// Use a ternary operator to assign a value to a new variable status
const status = age > 18 ? 'adult' : 'minor';

// Print the status variable to the console
console.log(status); // Output will be 'adult' if age is greater than 18, otherwise 'minor'
