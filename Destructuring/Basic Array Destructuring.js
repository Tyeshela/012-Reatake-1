// Given an array of three numbers
const numbers = [1, 2, 3];

// Array destructuring to create three variables
const [first, second, third] = numbers;

// Using rest parameter to capture remaining elements
const [firstTwo, ...rest] = [1, 2, 3, 4, 5];
