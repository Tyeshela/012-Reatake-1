// Create an array of numbers
const numbers = [1, 4, 9, 16, 25];

// Use the forEach method to log each number and its square root to the console
numbers.forEach(function(number) {
    console.log(`Number: ${number}, Square Root: ${Math.sqrt(number)}`);
});
